{% extends "SalatinoCMSBundle::layout.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{% block content %}
	{% block fos_user_content %}

	<div class="row">
		<div class="col-xs-12 col-md-10 col-md-offset-1">
			<div class="page-header">
				<h1>{{ 'titles.registration'|trans({}, 'CMS') }}</h1>
			</div>
		</div>

		<div class="col-xs-12 col-md-8 col-md-offset-2">

			{{ form_start(form, {'method': 'post', 'action': path('register_form'), 'attr': {'class': 'fos_user_registration_register'}}) }}

				{{ form_widget( form )  }}

			    <div>
			    	<button type="submit" class="btn btn-default pull-rigth">{{ 'registration.submit'|trans }}</button>
			    </div>
			{{ form_end(form) }}
		</div>
	</div>

	{% endblock %}
{% endblock %}